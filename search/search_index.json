{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to Error Translator","text":"<p>Error Translator is a local, rule-based Python error translator. It converts raw tracebacks into plain English, keeps the original file and line context when available, and returns a structured fix suggestion you can read in the terminal or consume from code.</p>"},{"location":"#why-it-exists","title":"Why it exists","text":"<p>Python tracebacks are precise, but they are not always beginner-friendly. This project narrows the gap by matching the final error line against a curated rule set, then combining that match with traceback metadata and optional AST-backed insight.</p> <p>Local by default</p> <p>Translation is deterministic and runs inside your Python process. No external service is required for normal CLI or library usage.</p>"},{"location":"#highlights","title":"Highlights","text":"<ul> <li>Clear explanations written for humans, not compiler output.</li> <li>Structured results that include the matched error, file, line, and code context when available.</li> <li>Three entry points: import hook, CLI, and FastAPI service.</li> <li>Extensible rule table stored in <code>error_translator/rules.json</code>.</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install error-translator-cli-v2\n</code></pre> <p>For local development, install the repository requirements instead:</p> <pre><code>pip install -r requirements.txt\n</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":""},{"location":"#automatic-interception","title":"Automatic interception","text":"script.py<pre><code>import error_translator.auto\n\nmaximum_user_connections = 100\nprint(maximum_user_connectons)\n</code></pre> <p>This is the magic import. Importing <code>error_translator.auto</code> installs the project's <code>sys.excepthook</code> handler, so unhandled exceptions in that process are translated automatically instead of showing only the raw traceback.</p> <p>Use it when you want the error translation to happen with a single import and no extra wrapper code.</p>"},{"location":"#cli-translation","title":"CLI translation","text":"<p>Run a script and translate any crash that appears on <code>stderr</code>:</p> <pre><code>explain-error run script.py\n</code></pre> <p>Translate a raw error string directly:</p> <pre><code>explain-error \"TypeError: unsupported operand type(s) for +: 'int' and 'str'\"\n</code></pre> <p>Pipe saved traceback text into the CLI:</p> <pre><code>Get-Content error.log | explain-error\n</code></pre> <pre><code>cat error.log | explain-error\n</code></pre>"},{"location":"#api-surface","title":"API surface","text":"<p>The FastAPI service exposes a lightweight HTTP interface for integrations and demos.</p> <pre><code>uvicorn error_translator.server:app --reload\n</code></pre> <p><code>POST /translate</code> accepts JSON with a <code>traceback_setting</code> field containing the traceback text.</p>"},{"location":"#returned-fields","title":"Returned fields","text":"<p>The translation result may include:</p> <ul> <li><code>explanation</code></li> <li><code>fix</code></li> <li><code>matched_error</code></li> <li><code>file</code></li> <li><code>line</code></li> <li><code>code</code></li> <li><code>ast_insight</code></li> </ul>"},{"location":"#learn-more","title":"Learn more","text":"<ul> <li>Read the architecture overview in <code>ARCHITECTURE.md</code>.</li> <li>See contribution guidance in <code>CONTRIBUTING.md</code>.</li> </ul>"},{"location":"#for-contributors","title":"For contributors","text":"<p>If you are here to improve the project, start with a small change. Good first contributions include fixing a doc typo, improving one rule explanation, or adding a test for an existing traceback example.</p> <p>Before you open a pull request, run <code>pytest</code> and check that the docs still describe the code accurately.</p>"},{"location":"ARCHITECTURE/","title":"System Architecture","text":"<p>This repository contains a single Python translation engine and a set of thin entry points around it. The runtime flow is intentionally simple: parse a traceback, match the final error line against the rule database, enrich the result with traceback context, and present the translation through the CLI, import hook, or HTTP API.</p>"},{"location":"ARCHITECTURE/#high-level-flow","title":"High-level flow","text":"<pre><code>flowchart TD\n    Input[Traceback text] --&gt; Core[core.translate_error]\n    Core --&gt; Rules[rules.json]\n    Core --&gt; AST[ast_handlers.py]\n    Core --&gt; Result[Structured translation dict]\n\n    Result --&gt; CLI[cli.py colorized output]\n    Result --&gt; Auto[auto.py sys.excepthook]\n    Result --&gt; API[server.py FastAPI response]\n</code></pre>"},{"location":"ARCHITECTURE/#runtime-modules","title":"Runtime modules","text":"Component Responsibility <code>error_translator/core.py</code> Loads <code>rules.json</code>, parses the traceback text, extracts the file name, line number, and code context, and returns the translation dictionary. <code>error_translator/cli.py</code> Implements the <code>explain-error</code> command. It supports raw traceback strings, piped input, and <code>run &lt;script.py&gt;</code> execution. <code>error_translator/auto.py</code> Replaces <code>sys.excepthook</code> so unhandled Python exceptions are translated automatically when the module is imported. <code>error_translator/server.py</code> Exposes the FastAPI app with <code>GET /</code> and <code>POST /translate</code>. <code>error_translator/ast_handlers.py</code> Provides optional contextual handlers for some error families such as <code>NameError</code>, <code>AttributeError</code>, and <code>ImportError</code>. <code>error_translator/rules.json</code> Stores the regex patterns, explanations, fixes, and default fallback message."},{"location":"ARCHITECTURE/#supporting-files","title":"Supporting files","text":"Component Responsibility <code>builder.py</code> Optional rule-authoring helper that asks Gemini to draft new regex rules from the scraped error dataset. It requires <code>GEMINI_API_KEY</code>. <code>scraper.py</code> Refreshes <code>scraped_errors_database.json</code> by reading the Python exception documentation from docs.python.org. <code>typo.py</code> Small sample script used to demonstrate the automatic import hook during development. <code>tests/test_core.py</code> Regression coverage for the translation pipeline and traceback parsing."},{"location":"ARCHITECTURE/#translation-steps","title":"Translation steps","text":"<ol> <li>Read the traceback text.</li> <li>Use the last non-empty line as the error line to match.</li> <li>Extract the file and line number from the traceback when present.</li> <li>Load <code>rules.json</code> and evaluate the regex rules in order.</li> <li>Format the explanation and fix with any captured values.</li> <li>Pull a source line from disk when the file and line are available.</li> <li>Ask <code>ast_handlers.AST_REGISTRY</code> for an additional insight when a matching handler exists.</li> <li>Return a dictionary that the CLI, import hook, and API can render consistently.</li> </ol>"},{"location":"ARCHITECTURE/#output-contract","title":"Output contract","text":"<p><code>translate_error()</code> returns a Python dictionary with a stable shape so every front end can reuse the same engine without extra translation work. The most important fields are <code>explanation</code>, <code>fix</code>, <code>matched_error</code>, <code>file</code>, <code>line</code>, and <code>code</code>; <code>ast_insight</code> is included when a handler produces one.</p>"},{"location":"ARCHITECTURE/#packaging","title":"Packaging","text":"<p>The package is exposed through <code>pyproject.toml</code> as the <code>explain-error</code> console script. There is no separate VS Code extension in this repository; the distributed package here is the Python CLI and API engine itself.</p>"},{"location":"CONTRIBUTING/","title":"Contributing to Error Translator","text":"<p>Thanks for helping improve the project. This guide is meant to make the first contribution feel straightforward, not formal.</p>"},{"location":"CONTRIBUTING/#start-here","title":"Start here","text":"<p>Set up the repository and make sure the tests pass before changing anything:</p> <pre><code>pip install -r requirements.txt\npytest\n</code></pre> <p>If that works, you are ready to contribute.</p>"},{"location":"CONTRIBUTING/#where-to-make-changes","title":"Where to make changes","text":"<ul> <li><code>error_translator/rules.json</code> for better explanations or fixes.</li> <li><code>error_translator/core.py</code> if the translation flow itself needs a change.</li> <li><code>error_translator/ast_handlers.py</code> if the suggestion needs source-file context.</li> <li><code>tests/test_core.py</code> to protect the behavior you changed.</li> <li><code>README.md</code> or <code>docs/</code> if the instructions are unclear or incomplete.</li> </ul>"},{"location":"CONTRIBUTING/#a-simple-contribution-flow","title":"A simple contribution flow","text":"<ol> <li>Choose one small issue to improve.</li> <li>Make the smallest change that solves it.</li> <li>Add or update a test if the behavior changed.</li> <li>Run <code>pytest</code>.</li> <li>Open the pull request with a short summary of what changed and why.</li> </ol>"},{"location":"CONTRIBUTING/#writing-a-better-rule","title":"Writing a better rule","text":"<ul> <li>Match the exact final traceback line when possible.</li> <li>Keep the regex narrow and specific.</li> <li>Write explanations in plain language.</li> <li>Make the fix direct and practical.</li> <li>Avoid adding extra noise to the rule if the current wording already works.</li> </ul>"},{"location":"CONTRIBUTING/#helpful-first-prs","title":"Helpful first PRs","text":"<ul> <li>Fix a typo in the docs.</li> <li>Improve one rule explanation.</li> <li>Add one missing test case.</li> <li>Clarify an example in the README.</li> </ul>"},{"location":"CONTRIBUTING/#optional-tools","title":"Optional tools","text":"<p>If you want a draft for a new rule, run <code>python builder.py</code> after setting <code>GEMINI_API_KEY</code>.</p> <p>If you want to refresh the scraped Python exception dataset, run <code>python scraper.py</code>.</p>"},{"location":"CONTRIBUTING/#before-you-open-a-pr","title":"Before you open a PR","text":"<ul> <li>The docs still match the code.</li> <li>The test suite passes locally.</li> <li>The change is focused and easy to review.</li> <li>Any new behavior has a test.</li> </ul>"}]}